import { $factory } from "../element/element-factory.js";

/**
 * Creates a Figure with Optional Caption HTML element.
 * @returns {HTMLElementTagNameMap["figure"]}
 */
export const $figure = $factory("figure");
